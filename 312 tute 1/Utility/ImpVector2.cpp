// Geometric Tools, LLC
// Copyright (c) 1998-2010
// Distributed under the Boost Software License, Version 1.0.
// http://www.boost.org/LICENSE_1_0.txt
// http://www.geometrictools.com/License/Boost/LICENSE_1_0.txt
//
// File Version: 5.0.0 (2010/01/01)

#include "ImpVector2.h"

/**
 * @fn	template<> const Vector2<float> Vector2<float>::ZERO(0.0f, 0.0f);
 *
 * @brief	Zer operating system.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	float	Type of the float.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<float> Vector2<float>::ZERO(0.0f, 0.0f);

/**
 * @fn	template<> const Vector2<float> Vector2<float>::UNIT_X(1.0f, 0.0f);
 *
 * @brief	Unit x coordinate.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	float	Type of the float.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<float> Vector2<float>::UNIT_X(1.0f, 0.0f);

/**
 * @fn	template<> const Vector2<float> Vector2<float>::UNIT_Y(0.0f, 1.0f);
 *
 * @brief	Unit y coordinate.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	float	Type of the float.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<float> Vector2<float>::UNIT_Y(0.0f, 1.0f);

/**
 * @fn	template<> const Vector2<float> Vector2<float>::ONE(1.0f, 1.0f);
 *
 * @brief	On es.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	float	Type of the float.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<float> Vector2<float>::ONE(1.0f, 1.0f);

/**
 * @fn	template<> const Vector2<float> Vector2<float>::GRAVITY(0.0f,-9.8f);
 *
 * @brief	Gravit ys.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	float	Type of the float.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<float> Vector2<float>::GRAVITY(0.0f,-9.8f);

/**
 * @fn	template<> const Vector2<double> Vector2<double>::ZERO(0.0,0.0);
 *
 * @brief	Zer operating system.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	double	Type of the double.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<double> Vector2<double>::ZERO(0.0,0.0);

/**
 * @fn	template<> const Vector2<double> Vector2<double>::UNIT_X(1.0,0.0);
 *
 * @brief	Unit x coordinate.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	double	Type of the double.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<double> Vector2<double>::UNIT_X(1.0,0.0);

/**
 * @fn	template<> const Vector2<double> Vector2<double>::UNIT_Y(0.0,1.0);
 *
 * @brief	Unit y coordinate.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	double	Type of the double.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<double> Vector2<double>::UNIT_Y(0.0,1.0);

/**
 * @fn	template<> const Vector2<double> Vector2<double>::ONE(1.0,1.0);
 *
 * @brief	On es.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	double	Type of the double.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<double> Vector2<double>::ONE(1.0,1.0);

/**
 * @fn	template<> const Vector2<double> Vector2<double>::GRAVITY(0,-9.8);
 *
 * @brief	Gravit ys.
 *
 * @author	Noel Waghorn
 * @date	19/09/2011
 *
 * @tparam	double	Type of the double.
 * @param		The.
 * @param		The.
 *
 * @return	.
 */

template<> const Vector2<double> Vector2<double>::GRAVITY(0,-9.8);
